#!/usr/bin/env -S just --justfile

set shell := ["fish", "-c"]

develop:
	nix develop --command fish

build:
	nix build
	nixfmtty flake.lock

test:
	nix develop --unset PATH --command ./scripts/test.fish

update:
	nix flake update
	nixfmtty flake.lock
